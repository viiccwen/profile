/** English translations merged by section for easier maintenance */
import { common } from "./common";
import { about } from "./about";
import { experience } from "./experience";
import { projects } from "./projects";

export const en = {
  translation: {
    ...common,
    ...about,
    ...experience,
    ...projects,
  },
};
